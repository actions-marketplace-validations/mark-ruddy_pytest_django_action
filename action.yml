# action.yml
name: 'pytest-django-action-stepped'
description: 'GitHub action to run pytest-django on your Django web app'
branding:
  icon: 'check-square'
  color: 'black'
runs:
  using: 'docker'
  image: 'Dockerfile'
